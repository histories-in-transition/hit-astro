---
import DownloadButtons from "./download-buttons.astro";

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<div class="mx-auto md:w-3/4">
	<div class="flex justify-between align-middle mb-2">
		<h1 class="text-2xl font-semibold text-brandRed py-2">{title}</h1>
		<DownloadButtons />
	</div>
	<div class="text-brandRed text-xl">
		Es werden
		<span id="counter1"></span> von
		<span id="counter2"></span> Einträgen angezeigt
	</div>
	<div class="overflow-auto max-h-[1000px]">
		<table id="tabulator-table"></table>
	</div>
</div>
